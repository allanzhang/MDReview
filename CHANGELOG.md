# Changelog

## V1.6.0 — 2026-09-17

中英双语界面版本：语言默认跟随系统，也可手动固定并持久化。

### 语言
- View 菜单新增 Language 子菜单：跟随系统 / 中文 / English，当前选项带勾选标记
- 默认跟随系统：系统首选语言以 `zh` 开头显示简体中文，其他语言显示英文
- 手动选择会持久化，重启后保持；运行中切换立即生效，不重启、不重载文档、不改变阅读位置
- 「跟随系统」模式下系统语言变化立即跟随；手动固定后不受系统语言影响
- 覆盖主菜单、上下文菜单、工具栏、工具提示、About、更新提示与弹窗等全部自定义文案；Markdown 文档内容不翻译

### 关于
- About 页新增 `© 2026 MDReview` 与 GitHub 项目入口按钮（中文「GitHub 项目主页」/ 英文「GitHub Project」）

### 修复
- 修复中文系统首次启动显示英文：语言解析改读 macOS 全局语言偏好，并清理旧版本写入的应用域语言覆盖
- 修复切换语言后 macOS 系统菜单仍为中文：菜单项同步改写 `title`、`attributedTitle` 与顶层 `submenu` 标题，并按 action 识别 `Enter Full Screen` 等系统项
- 修复操作界面时菜单栏闪回中文：菜单只在主菜单层级变化时改写，且菜单依赖从应用状态解耦为「语言模式 + 实际语言 + 最近文件」

### 界面
- 侧栏与内容区配色分层：浅色模式侧栏改为浅灰面板色，与白色内容区形成分界

### 测试
- 新增应用语言解析、语言状态持久化、菜单本地化、About 链接与本地化资源完整性测试

## V1.5.2 — 2026-09-14

### 修复
- 修复外部编辑器原子替换保存后文件监听失效，导致刷新按钮红点只出现一次的问题
- 新增连续原子保存回归测试

### 文档
- 精简 README，并同步当前功能、安装、开发与发布说明

## V1.5.1 — 2026-09-14

### 阅读工具
- 文件更新不再自动重载；外部编辑器保存后在刷新按钮显示红点，用户手动刷新后加载内容并清除标记

## V1.5.0 — 2026-09-09

应用内自动更新版本：启动检查、手动检查、安全下载与安装重启。

### 更新与关于
- 新增自定义 About MDReview 页面，显示当前短版本与 Build
- 每次启动自动检查一次 GitHub 官方稳定 Release；失败静默，不阻塞阅读
- 新增 About 页与 App 菜单的手动 `Check for Updates…`
- 发现新版本时弹出版本确认框；`Later` 在本次启动内抑制后续自动提示
- 仅使用官方 `allanzhang/MDReview` 稳定 Release 与固定 `MDReview-<version>.zip` 资产
- 下载后校验 GitHub `sha256:` digest、Bundle ID 与更高版本
- 安装前检测目标目录可写性；通过临时 helper 等待退出、备份替换、失败回滚并重启

### 测试
- 新增 tag 解析与数字版本比较测试
- 新增流式 SHA256 文件摘要测试，避免大 ZIP 整包进入内存
- 既有渲染器回归测试与 CommonMark 门禁继续通过

## v1.4.1 — 2026-09-09

渲染稳定性与回归门禁修复版。

### 渲染
- 修复 `**"..."**` 等标点紧邻粗体边界时的配对错误，避免跨段吞并后续文本
- 标点判定扩展到 Unicode P/S 全集，覆盖 CJK 标点、符号和非 BMP 字符
- 普通粗体继续交给 markdown-it 标准规则，仅在标准 flanking 无法配对时启用兜底

### 测试与门禁
- 新增 CommonMark 0.31.2 官方 652 例一致性测试，5 条 App 已知差异单独登记
- 新增 35 条 golden fixtures、85,113 组 Unicode 标点边界和 13 条 App 扩展语法测试
- 新增 GitHub Actions：自动执行渲染测试和 macOS App 编译
- 新增 `Docs/renderer-contract.md`，固定渲染器基线、扩展语法、差异和改动门禁

## v1.4 — 2026-09-06

渲染与阅读工具修复版：内嵌 base64 图片识别 + 内容区手动刷新按钮。

### 渲染
- 修复文档中用 base64 内嵌图片（`![...](data:image/svg+xml;base64,...)`）被当成无效链接、整段按字面量显示的问题：`validateLink` 由仅放行 gif/png/jpeg/webp 泛化为任意 `data:image/*` 子类型
- 安全兜底不变：`javascript:` / `file:` / `vbscript:` 等链接仍拦截

### 阅读工具
- 工具栏搜索按钮后新增**刷新按钮**：重新读盘并重渲染，作为外部编辑器改动但文件监听未触发时的手动兜底；无文档时按钮禁用
- 刷新时内容有变化走既有重渲染链路、未变化也强制重渲染，不重复渲染

## v1.3.1 — 2026-09-04

渲染修复版：货币金额不再被误判为数学公式。

### 渲染
- 修复 `$60B`、`$2.7B`、`$6B` 等货币金额被误当成 `$...$` 数学公式、导致整段渲染混乱、粗体无法识别的问题
- 行内 `$` 后紧跟数字即视为金额（dollar 仅出现在前面、没有成对收尾），不再贪婪合并吞掉 `**...**` 粗体
- 真实公式（`$x^2$`、`$y = mx + b$`、`$$...$$`）不受影响

## v1.3.0 — 2026-09-02

快读体验版本：加载骨架替代白屏等待、浮字号调节、内链本地打开、代码块折叠、热更新提示。理念不变——快速、简洁、克制。

### 阅读体验
- **大纲思写 loading 骨架**：打开大文档时预扫描标题逐条淡入 + 顶部 2pt 扫描细线，替代白屏等待；无转圈、无伪进度，渲染完成整体淡出（与正文底色一致，无闪烁）
- **浮字号调节**：正文右上角 A−/A+ 竖排胶囊（0.8×~1.5×、步进 0.15、单击约 15%），直接改写 CSS 变量即时生效，不重载页面、不丢滚动位置；按钮带 hover 圆角底与按压反馈
- **Outline 左边距紧凑化**：H1 文字起始约 26pt，接近 Recent 内容区；H3+ 与 H2 同缩进，层级只靠字号/字重/文字色区分
- **代码块折叠**：点击语言栏整体折叠/展开，折叠态语言栏显示 `▸ 语言 · N lines`；正文/行号栏/复制按钮的点击不误触

### 链接与 AI 工作流
- **本地 .md 内链在 app 内打开**：`[x](other.md)`（相对/绝对路径、%20 自动解码）点击直接换读新文档，阅读流不断；其他本地文件点击在 Finder 定位，http(s)/mailto 转系统浏览器，WebView 不再整页导航离开阅读流
- **热更新提示**：外部编辑器保存自动重载后右上角闪现 2s「Updated」微型标签，连续更新自动续闪
- **Copy as Quote**：右键菜单新增，选中正文一键转 `> 引用` 格式（空行用 `>` 保持引用块连续），直接粘贴给 AI 当上下文

## v1.2.1 — 2026-08-24

本次是 V1.2.0 后的渲染与视觉修复版：扩展标点粗体兜底覆盖范围，并弱化阅读进度条视觉强度。

### 渲染
- 修复 `**"..."**` 等引号粗体显示裸星号的问题
- 标点粗体兜底从 CJK 引号扩展到 CommonMark 阻断强调的标点/符号全集，覆盖 ASCII 与 Unicode 常用标点
- 兜底解析异常时回退默认文本渲染，保留源码显示且不中断页面

### 阅读界面
- 阅读进度条由 3pt 渐变光效改为 2pt 中性纯色
- 浅色使用冷灰 `#c5cad3`，深色使用中性灰 `#525252`，亮暗主题下都清晰但不刺眼

## v1.2.0 — 2026-08-18

本次以左侧 Outline 大纲导航面板优化与全 app Codex 式中性实底风格统一为主；右侧文档内容区域仅调整配色（暗色提亮去蓝、代码高亮换 MDReview 统一调色板），布局与交互不变。

### Outline 大纲
- Outline / Recent 切换改为中性灰轨道 + 实底滑块（无 accent 蓝），移除标签栏下硬分隔线
- 侧栏与工具栏改为不透明实底（windowBackgroundColor），移除系统磨砂玻璃材质，厚实清晰
- 标题树层级：缩进 + 字号 + 字重 + 文字色四重区分 H1~H4+（浅色 H1 13pt semibold #262626 / H2 12.5pt medium #3D3D3D / H3 12pt #545454 / H4+ 11.5pt #737373，整体提亮减重；深色保持原有亮度梯度）
- 有子标题的节点渲染折叠/展开三角（▶/▼），点击可折叠收起子树；叶子节点不显示三角
- 大纲行整行响应点击（含缩进条与留白区域），点击即跳转，灵敏无死区
- 标题强制单行显示，超长文本末尾截断为 …，hover 弹出 tooltip 展示完整标题
- 选中态改为 Codex 式中性灰圆角底（浅色 black 9% / 深色 white 14%），文字保持原色、不再反白，高亮不再全栏铺满
- hover 态为更浅的中性灰圆角底（浅色 black 4% / 深色 white 7%），文字颜色保持不变
- 点击大纲条目平滑滚动到对应标题；正文滚动时大纲自动高亮当前章节，并跟随滚动保证激活条目始终可视
- 源码视图的大纲点击跳转同步改为平滑滚动

### 界面与交互
- Outline 面板隐藏滚动指示器，滚动条不再扎眼
- 修复搜索框点击 × 关闭按钮无法清空已输入关键字的问题

### 代码高亮
- 弃用 Atom One Light / Dark，改为 MDReview 统一调色板：蓝=关键字、绿=字符串与标签、琥珀=数字常量、紫=函数类型、灰=注释，仅四个低饱和色族
- 浅色 / 深色为同一组色相的明暗变体，切换主题时观感一致，消除"彩虹式"杂乱
- 全部 token 色按真实容器背景校验对比度（浅色 ≥ 4.5:1、深色 ≥ 5.2:1），注释不再看不清
- 关键字不再加粗，仅保留 section 加粗与注释斜体，视觉更干净
- 浅色代码块容器去蓝并加深：背景 #eef3fb → #e7eaed、语言栏/行号栏 #e7eef9 → #dfe3e7、行内代码底 #e3ebf7 → #e5e8eb，块状边界清晰可辨，与高亮配色和谐

### 暗色主题（仅调色，不改内容区布局）
- 正文背景统一为中性灰 #262626（原 #1f1f1f），正文 #d8d8d8、标题 #ececec，整体提亮一档，去除发暗发"脏"观感
- 代码块容器去蓝并与浅色镜像对称：背景 #1d2635 → #1d1d1d（比页面沉一档）、行号/语言栏 #212c3e → #242424、边框改中性灰，块状边界清晰
- 行内代码芯片提亮：#313131 → #404040（比页面亮一档，与浅色 #e5e8eb 镜像），表格/正文中的 code 边界一眼可辨
- Source 视图暗色背景同步提亮至 #262626，与渲染视图一致
- 浅色主题完全不变


## v1.1.3 — 2026-08-17

V1.1.3 是 V1.1.0 后的收尾优化版：原生搜索、源码搜索、渲染细节与交互一致性全部定版。

### 搜索
- 悬浮搜索面板替换为工具栏原生 `NSSearchField`，点击搜索按钮展开，`⌘F` 聚焦并预填选中文字
- 搜索计数显示在搜索框内部，清空按钮只清内容，失去焦点自动收起
- Source view 搜索改走 NSTextView：匹配计数、跳转、系统查找指示器、选中文字预填
- 显式补齐 Copy / Paste / Select All 系统菜单命令，`⌘C` / `⌘V` 稳定响应

### 渲染
- 修复 `**「...」**` 等 CJK 标点粗体：保留标准 markdown-it 解析，仅对残留文本 token 做最小兜底
- 表格宽度自适应页宽，超宽内容内部换行，不再横向滚动；表格脚注字号与表格一致
- 代码块长行自动折行，不出现横向滚动条
- 代码字体改为优先 Consolas，高亮主题换成 Atom One Light / Dark，撞色更明显
- 代码块背景统一为蓝灰系，和系统控件、Outline 高亮属于同一色系
- 标题 `#` 锚点移除，标题与正文绝对左对齐
- 大纲点击与滚动高亮共用实时标题坐标，窗口变化自动重算

### 界面
- Outline 行高压缩，列表更紧凑
- Outline 点击驱动选中，滚动反向同步高亮；程序化滚动短暂锁定，避免点击后被滚动事件抢走选中
- 紧邻标题（如 1.2 / 1.3、2 / 2.1）的 active 判定优化，不再误选中下一项
- Rendered / Source 字体深浅独立调优：Rendered 更清晰，Source 保持只读质感

## v1.1.0 — 2026-08-17

V1.1.0 是视觉与渲染细节的大版本：排版更干净，代码块和表格更精致，并修复 CJK 标点粗体与复制串块问题。

### 渲染
- 修复 `**「...」**`、`**...）**` 等 CJK 标点紧邻粗体时无法渲染的问题
- 表格：圆角边框、细分横竖线、浅灰表头、行 hover、紧凑行高；表格字号比正文小一号
- 代码块：语言栏 + 行号 + 右上角 Copy 按钮，点击有反馈；多代码块复制互不串块
- 代码/表格滚动条细化为 8px 圆角样式，亮暗主题均适配
- 任务 checkbox 改为静态禁用样式，前置圆点并与列表缩进对齐

### 阅读与排版
- 正文 14.5px 基准，标题/正文全部左对齐，隐藏锚点不再造成偏移
- 标题与段落间距压缩，两侧留白加大到 8%，阅读更紧凑透气
- macOS 抗锯齿优化，文字颜色平衡为“比纯黑稍浅”，不再发灰或压抑
- 标题后的引用/作者块去除背景和横线，保留紧凑纯文本
- 引用卡片、图片阴影、公式留白统一精细
- 阅读进度条改为渐变胶囊并带轻微阴影

### 界面
- 大纲 hover/active 动效更顺滑
- 暗色主题代码栏、行号、表格 hover、checkbox、滚动条独立调成灰阶

## v1.0.1 — 2026-08-17

V1.0.1 聚焦阅读体验和渲染稳定性：表格列宽改为内容自适应测宽，源码/渲染视图的滚动、进度、大纲高亮彻底分离，并修复 Recent 列表刷新问题。

### 渲染
- 表格列宽内容测宽：按每列 `max-content` 计算理想宽度，写入 `colgroup` + `table-layout: fixed` 落地
- 列宽加入最小/最大比例约束，窄容器下最小约 120px，避免首列被挤压或单列独占
- 含图片、`colspan`、`rowspan` 的表格自动回退原自动布局，避免误伤特殊表格

### 阅读
- Source / Rendered 视图滚动位置独立记忆，来回切换不再互相污染
- 阅读进度与大纲高亮按当前视图独立管理，rendered 精确恢复真实 `scrollY`
- Source 视图滚动位置按文档持久化，重开/切换后恢复
- 大纲点击在 Source 视图内跳转源码位置，Rendered 视图跳转渲染位置
- 大纲激活项文字柔和化，选中态更贴近阅读视觉

### 界面
- 工具栏新增纯图标 Source / Rendered 切换按钮，与主题高亮一致
- 阅读区右键菜单同步提供 Source / Rendered 切换
- 修复删除 Recent 后侧边栏列表不刷新的问题

## v1.0.0 — 2026-08-14

首个正式版本。轻量 Markdown 审阅器，SwiftUI + WKWebView 原生 macOS 应用，全部依赖离线内置。

### 渲染
- GFM 全语法：表格 / 任务列表 / 删除线 / 脚注，单换行即换行
- KaTeX 数学公式（`$...$` / `$$...$$`），markdown-it 拦截保护防破坏
- Mermaid 图表按需渲染，失败降级保留原代码
- highlight.js 36 语言代码高亮，亮/暗双主题，大文档分帧渐进渲染
- 扩展语法：emoji、`==高亮==`、上下标、定义列表、单波浪线删除线（`~文字~` / `H~2~O`）
- 标题锚点、图片懒加载、正文字号跟随系统

### 阅读
- 大纲双向同步：侧栏点选跳转，滚动反向高亮当前章节
- 超大文档分块懒渲染（>25 万字符自动按标题切分）
- 全文搜索：`⌘F` 悬浮面板、Enter/Shift+Enter 上下跳转、Esc 关闭、命中计数、无匹配提示、选中文字自动预填
- 阅读进度记忆（UserDefaults 持久化，重开自动回到上次位置）
- 文件热更新：外部编辑器保存后自动重载
- 右键菜单：正文（复制 / 显示位置 / 外部编辑器 / 导出）、最近文件、大纲

### 外观
- 三态外观：跟随系统 / 临时亮 / 临时暗
- dark 主题 VS Code「Dark Modern」调色
- 阅读进度条、窗口标题（文件名 + 目录 + 字数）

### 打开与导出
- 打开：工具栏 / `⌘O` / 拖拽 / Finder 关联 / Open Recent；启动恢复上次文档与窗口状态
- 导出 HTML（无 JS 静态快照，Preview/QuickLook 可开）与 PDF（WKWebView 原生分页）
- 导出成功弹窗，一键在 Finder 中显示

### 界面
- 标准菜单栏（File / View）与精简工具栏
- 空状态引导、App 图标（标准全尺寸集）
- 单窗口架构，不支持多 Tab（第一版有意为之）
